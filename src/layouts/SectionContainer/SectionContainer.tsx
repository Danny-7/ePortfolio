import React from 'react'

const SectionContainer = ({ content }: { content: React.ReactElement }) => {
    return <section className="section-container">{content}</section>;
}

export default SectionContainer
